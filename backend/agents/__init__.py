"""Broker Workbench AI Agents.

The active agent path is the Microsoft Agent Framework HandoffBuilder
workflow under ``backend.agents.foundry``. Specialist factory functions
are exposed there. Tools are served via the standalone MCP server
(``mcp_server.tools``) and consumed by the workflow over MCP.
"""
