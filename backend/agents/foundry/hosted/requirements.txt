# Hosted-agent container deps. Pin same Agent Framework version as backend
# (see backend/requirements.txt) so the handoff workflow code is binary-compatible.
agent-framework>=1.6.0,<2
agent-framework-orchestrations>=1.0.0rc2
agent-framework-openai
agent-framework-foundry-hosting
azure-identity>=1.17.0
python-dotenv>=1.0.0
# MCP tool client (used by InstrumentedMCPTool)
mcp>=1.0.0
