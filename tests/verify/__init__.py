# tests/verify/ — runtime verification tests that target a deployed stack
# (Hosted agent, DirectLine, App Insights). Distinct from tests/eval/ which
# focuses on prompt-quality regression and tests/agents/ which is unit-level.
