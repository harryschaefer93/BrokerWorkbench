[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py
pythonpath = . backend
addopts = -m "not live"
markers =
    live: live tests hitting real Azure OpenAI; opt-in via -m live
filterwarnings =
    ignore::DeprecationWarning
